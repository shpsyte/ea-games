import { fireEvent, render } from '@testing-library/react'
import React from 'react'

describe('This is just a test', () => {
  it('should log passed', () => {
    // just a test
    expect(true).toBe(true)
  })
})
